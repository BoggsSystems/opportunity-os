# Logical Relational Model

The canonical logical relational model now lives in [LOGICAL_MODEL.md](./LOGICAL_MODEL.md).

This file is intentionally kept as a pointer so existing references do not break while the project uses a single source of truth for logical schema design.
